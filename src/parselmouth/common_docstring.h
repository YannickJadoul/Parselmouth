#pragma once

namespace parselmouth
{
constexpr auto LEN_DOCSTRING = R"(Return len(self).)";
constexpr auto GETITEM_DOCSTRING = R"(x.__getitem__(y) <==> x[y])";
constexpr auto ITER_DOCSTRING = R"(Implement iter(self).)";
}